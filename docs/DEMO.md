# Demo & Testing Guide

## 빠른 데모

시스템이 어떻게 작동하는지 단계별로 확인해봅시다.

### Step 1: 시스템 시작

```bash
cd MAS
./run.sh
```

메뉴가 표시되면 `1) 터미널 실행`을 선택합니다.

**예상 출력**:
```
터미널 모드 시작...

[1/4] 워크스페이스 초기화...
[2/4] 이전 작업 정리...
[3/4] 에이전트 설정...
[4/4] 기존 세션 정리...

에이전트 세션 시작...
✓ 모든 에이전트 세션이 시작되었습니다 (9/9)
```

### Step 2: Orchestrator 접속

세션 모니터가 표시되면 `1`을 눌러 Orchestrator 세션에 접속합니다.

```
세션 상태 모니터

  모드: 터미널
  에이전트 세션: 9/9 실행 중

  에이전트 상태:
    ● orchestrator
    ● requirement-analyst
    ● ux-designer
    ...

  1) Orchestrator      4) Tech Architect   7) Developer
  2) Requirement       5) Planner          8) Reviewer
  3) UX Designer       6) Test Designer    9) Documenter

  0) 모든 에이전트 보기 (3x3 그리드)

  s) 상태 새로고침
  q) 세션 종료 및 메인 메뉴로
```

Orchestrator 세션에 접속하면 프로젝트 요청을 입력할 수 있습니다.

### Step 3: 프로젝트 요청

**테스트 케이스 1: 간단한 프로젝트**
```
"TODO 리스트 웹 앱 만들어줘"
```

**예상 진행**:
1. Orchestrator가 요청 저장
2. requirement-analyst 세션에 작업 지시
3. 몇 분 후 requirement-analyst가 질문 반환:
   ```
   📋 요구사항 분석 완료
   
   다음 사항을 확인해주세요:
   1. 항목 추가/삭제/완료 표시 기능이 필요한가요?
   2. 로컬 저장 방식은? (localStorage? 서버?)
   3. 카테고리 분류 기능이 필요한가요?
   ...
   ```

**테스트 케이스 2: 복잡한 프로젝트**
```
"3D로 주사위를 굴릴 수 있는 웹 만들어줘"
```

### Step 4: 다른 세션 모니터링

orchestrator 세션에서 `Ctrl+b, d`를 눌러 나온 후:

```bash
# 모든 세션 보기
tmux list-sessions

# requirement-analyst 세션 확인
tmux attach-session -t requirement-analyst

# developer 세션 확인
tmux attach-session -t developer

# 다시 orchestrator로
tmux attach-session -t orchestrator
```

### Step 5: 워크스페이스 확인

다른 터미널에서:

```bash
# 에이전트 상태 실시간 모니터링
watch -n 2 'cat workspace/status/*.status'

# 작업 큐 확인
watch -n 2 'ls -la workspace/tasks/*/'

# 생성된 산출물 확인
ls -la workspace/artifacts/

# 로그 확인
tail -f workspace/logs/orchestrator.log
```

## 테스트 시나리오

### 시나리오 1: 완전 자동 모드 (사용자 개입 최소)

```
1. 프로젝트 요청: "계산기 웹 앱"
2. 요구사항 질문 → 모두 "기본값 사용" 답변
3. 계획 승인 → "승인"
4. 구현 대기 → 자동 진행
5. 최종 확인 → "확인"

예상 소요: 30분 - 1시간
```

### 시나리오 2: 상세 커스터마이징 모드

```
1. 프로젝트 요청: "3D 주사위 굴리기"
2. 요구사항 상세 답변:
   - 물리 엔진: 사실적
   - 주사위 개수: 2개
   - 결과 히스토리: 필요
   - 스타일: 미니멀
3. UX 설계 검토 후 피드백
4. 각 iteration 완료 후 확인
5. 최종 검토 및 수정 요청

예상 소요: 3-4시간
```

### 시나리오 3: 실패 복구 테스트

```
1. 프로젝트 시작
2. developer 세션 강제 종료: `tmux kill-session -t developer`
3. orchestrator가 에러 감지
4. 사용자에게 알림
5. developer 재시작
6. 마지막 체크포인트부터 재개

목적: 장애 복구 메커니즘 검증
```

## 디버깅 체크리스트

### 문제: 세션이 시작되지 않음

```bash
# 1. tmux 실행 확인
tmux -V

# 2. gemini 명령어 확인
which gemini

# 3. 권한 확인
ls -la run.sh scripts/

# 4. 재시작
./scripts/stop-all.sh
./run.sh
```

### 문제: 에이전트가 응답하지 않음

```bash
# 1. 에이전트 상태 확인
cat workspace/status/[agent].status

# 2. 해당 세션 접속
# 세션 모니터에서 해당 번호 입력 또는:
tmux attach-session -t [agent]

# 3. 작업 큐 확인
ls workspace/tasks/[agent]/

# 4. 시그널 확인
ls workspace/signals/
```

### 문제: 작업이 멈춘 것 같음

```bash
# 1. 대기 중인 작업 확인
find workspace/tasks -type f -name "*.json"

# 2. 블로킹된 에이전트 찾기
grep "working" workspace/status/*.status

# 3. 세션 모니터에서 s 키로 상태 새로고침

# 4. 수동 시그널 전송 (긴급)
echo "status:completed" > workspace/signals/[task]-done
```

## 성능 측정

### 벤치마크 프로젝트

```bash
# 1. 간단한 프로젝트
time ./run.sh << EOF
TODO 리스트 웹 앱
기본값 사용
승인
확인
EOF

# 2. 중간 복잡도
time ./run.sh << EOF
타이머 앱
기본값 사용
승인
확인
EOF

# 3. 복잡한 프로젝트
time ./run.sh << EOF
데이터 시각화 대시보드
기본값 사용
승인
확인
EOF
```

### 메트릭 수집

```bash
# 에이전트별 평균 처리 시간
grep "completed" workspace/logs/*.log | \
  awk '{print $1, $2}' | \
  sort | uniq -c

# 총 API 호출 횟수
grep "API call" workspace/logs/*.log | wc -l

# 에러 발생 빈도
grep "error" workspace/logs/*.log | wc -l
```

## 고급 테스트

### 병렬 처리 테스트

```python
# test_parallel.py
import subprocess
import time

# 2개 프로젝트 동시 시작 (현재 미지원이지만 테스트)
p1 = subprocess.Popen(['./run.sh'])
time.sleep(5)
p2 = subprocess.Popen(['./run.sh'])

# 예상: 두 번째는 대기 또는 거부
```

### 부하 테스트

```bash
# 큰 프로젝트 요청
./run.sh << EOF
복잡한 SPA 애플리케이션 만들어줘:
- 사용자 인증
- 실시간 채팅
- 파일 업로드
- 데이터 시각화
- 반응형 디자인
...
EOF
```

### 엣지 케이스

```bash
# 1. 빈 요청
./run.sh << EOF

EOF

# 2. 매우 모호한 요청
./run.sh << EOF
뭔가 멋진 거 만들어줘
EOF

# 3. 불가능한 요청
./run.sh << EOF
시간 여행 머신 웹 앱
EOF
```

## 데모 스크립트

자동으로 전체 프로세스를 시연하는 스크립트:

```bash
# demo.sh
#!/bin/bash

echo "🎬 Multi-Agent System 데모 시작"
echo ""

# 1. 시스템 시작
echo "1️⃣ 시스템 초기화..."
./run.sh &
MAIN_PID=$!

sleep 30

# 2. 프로젝트 요청 (자동 입력)
echo "2️⃣ 프로젝트 요청 전송..."
tmux send-keys -t orchestrator:0 "간단한 카운터 웹 앱" Enter

sleep 60

# 3. 요구사항 승인 (기본값)
echo "3️⃣ 요구사항 승인..."
tmux send-keys -t orchestrator:0 "모두 기본값으로 진행" Enter

sleep 120

# 4. 계획 승인
echo "4️⃣ 구현 계획 승인..."
tmux send-keys -t orchestrator:0 "승인" Enter

# 5. 진행 상황 모니터링
echo "5️⃣ 진행 상황 모니터링..."
watch -n 5 'cat workspace/status/*.status'

wait $MAIN_PID
```

## 예상 결과물

### 성공 시

```
workspace/
├── project/
│   └── counter-app/
│       ├── index.html
│       ├── src/
│       │   ├── App.jsx
│       │   ├── Counter.jsx
│       │   └── styles.css
│       └── package.json
├── docs/
│   ├── README.md
│   ├── ARCHITECTURE.md
│   └── API.md
└── tests/
    └── Counter.test.jsx
```

### 실패 시

```
workspace/
├── logs/
│   ├── error.log
│   └── orchestrator.log (에러 상세)
└── status/
    └── failed_phase.txt
```

## 트러블슈팅 FAQ

**Q: "gemini: command not found"**
```bash
# Gemini CLI 설치
npm install -g @google/gemini-cli
```

**Q: tmux 세션이 너무 많아요**
```bash
# 모두 종료
tmux kill-server

# 다시 시작
./run.sh
```

**Q: 특정 단계에서 멈췄어요**
```bash
# 에이전트 상태 확인
cat workspace/status/*.status

# 세션 모니터에서 q로 종료 후 재시작
./run.sh
```

**Q: 비용이 얼마나 드나요?**
```
- 간단한 프로젝트: ~$0.50-$2
- 중간 복잡도: ~$3-$10
- 복잡한 프로젝트: ~$10-$50

(Gemini API 요금 기준, 2025년)
```

## 다음 단계

✅ 데모 완료했다면:
1. 실제 프로젝트 시도
2. GEMINI.md 커스터마이징
3. 워크플로우 조정
4. 자동화 레벨 최적화

문제나 제안사항이 있으면 이슈로 올려주세요!
