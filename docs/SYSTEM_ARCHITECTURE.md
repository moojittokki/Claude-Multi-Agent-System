# Multi-Agent Development System - 시스템 아키텍처 문서

> 본 문서는 현재 구현된 시스템을 면밀히 분석하여 작성되었습니다.
> 작성일: 2025-12-10

## 목차

1. [시스템 개요](#1-시스템-개요)
2. [에이전트 구성](#2-에이전트-구성)
3. [통신 메커니즘](#3-통신-메커니즘)
4. [전체 워크플로우](#4-전체-워크플로우)
5. [각 에이전트 상세](#5-각-에이전트-상세)
6. [파일 시스템 구조](#6-파일-시스템-구조)
7. [토큰 소모 및 성능](#7-토큰-소모-및-성능)

---

## 1. 시스템 개요

### 1.1 핵심 개념

Multi-Agent Development System은 **9개의 독립적인 AI 에이전트**가 협업하여 소프트웨어 개발을 자동화하는 시스템입니다.

**핵심 특징:**
- **파일 기반 통신**: JSON 작업 파일 + 텍스트 시그널 파일
- **비동기 대기**: Bash 명령어로 시그널 파일 폴링 (최대 6분 타임아웃)
- **TDD 강제**: Test Designer → Developer → Reviewer 순서 강제
- **프로젝트 격리**: 각 프로젝트는 `/workspace/project/[타임스탬프]_[이름]/`에 독립 저장

### 1.2 기술 스택

```
- 에이전트 실행 환경: Gemini CLI
- 세션 관리: tmux
- 통신 방식: 파일 시스템 (JSON, 텍스트)
- 스크립트: Bash
```

---

## 2. 에이전트 구성

### 2.1 에이전트 목록 및 역할

| 번호 | 에이전트 | 역할 | 주요 산출물 |
|------|----------|------|-------------|
| 1 | **orchestrator** | 중앙 제어, 워크플로우 관리 | 없음 (조율만) |
| 2 | **requirement-analyst** | 요구사항 분석 및 명확화 | requirements-draft.md, requirements.md |
| 3 | **ux-designer** | UX/UI 설계 | ux-design.md |
| 4 | **tech-architect** | 기술 스택 및 아키텍처 설계 | tech-spec.md |
| 5 | **planner** | 구현 계획 수립 (Iteration 분할) | implementation-plan.md |
| 6 | **test-designer** | 테스트 케이스 작성 (TDD) | test-plan.md, tests/ |
| 7 | **developer** | 실제 코드 구현 | /workspace/project/[project]/ |
| 8 | **reviewer** | 코드 리뷰 및 품질 검증 | code-review-iter[N].md |
| 9 | **documenter** | 프로젝트 문서화 | README.md, ARCHITECTURE.md |

### 2.2 에이전트 실행 방식

각 에이전트는 **독립된 tmux 세션**에서 실행됩니다:

```bash
# 예: orchestrator 에이전트 시작
tmux new-session -d -s orchestrator -c workspace/agents/orchestrator
tmux send-keys -t orchestrator:0 "gemini --dangerously-skip-permissions --append-system-prompt \"\$(cat GEMINI.md)\""
sleep 0.2
tmux send-keys -t orchestrator:0 C-m
```

**시스템 프롬프트 로딩:**
- 각 에이전트 폴더의 `GEMINI.md` 파일이 시스템 프롬프트로 사용됨
- `--append-system-prompt` 옵션으로 기본 프롬프트에 추가

---

## 3. 통신 메커니즘

### 3.1 파일 기반 통신 구조

```
/workspace/
├── tasks/                    # 작업 지시 (Orchestrator → Agent)
│   ├── requirement-analyst/
│   │   └── task-001.json     # JSON 형식의 작업 파일
│   ├── ux-designer/
│   └── ...
│
├── signals/                  # 완료 시그널 (Agent → Orchestrator)
│   ├── req-analysis-done     # 텍스트 형식의 시그널
│   ├── ux-design-done
│   └── ...
│
├── status/                   # 에이전트 상태
│   ├── orchestrator.status   # "idle" or "working"
│   ├── requirement-analyst.status
│   └── ...
│
├── input/                    # 입력 데이터
│   └── user_request.txt      # 사용자 요청
│
└── artifacts/                # 중간 산출물
    ├── requirements-draft.md
    ├── requirements.md
    ├── ux-design.md
    └── ...
```

### 3.2 작업 지시 형식 (Task File)

**예: `/workspace/tasks/requirement-analyst/task-001.json`**

```json
{
  "task_id": "req-analysis-001",
  "command": "analyze_requirements",
  "input": "/workspace/input/user_request.txt",
  "output": "/workspace/artifacts/requirements-draft.md",
  "callback": "/workspace/signals/req-analysis-done"
}
```

**필드 설명:**
- `task_id`: 작업 고유 ID
- `command`: 수행할 작업 종류
- `input`: 입력 파일 경로
- `output`: 결과 파일 저장 경로
- `callback`: 완료 시그널 파일 경로

### 3.3 시그널 형식 (Signal File)

**예: `/workspace/signals/req-analysis-done`**

```
status:need_user_input
artifact:/workspace/artifacts/requirements-draft.md
question_count:7
timestamp:2025-12-10T15:30:00+09:00
```

**필드 설명:**
- `status`: completed | need_user_input | error | approved | rejected
- `artifact`: 산출물 파일 경로
- 기타 필드: 작업별로 다름 (question_count, blocking_issues 등)

### 3.4 통신 프로토콜 흐름

```
┌─────────────────┐
│  Orchestrator   │
└────────┬────────┘
         │
         │ 1. 에이전트 상태 확인
         │    while [ "$(cat status/requirement-analyst.status)" != "idle" ]; do sleep 1; done
         │
         │ 2. 작업 파일 생성
         │    cat > tasks/requirement-analyst/task-001.json << TASK
         │    { "task_id": "...", ... }
         │    TASK
         │
         │ 3. 상태 업데이트
         │    echo "working" > status/requirement-analyst.status
         │
         │ 4. tmux 알림 전송 (메시지와 Enter 분리)
         │    tmux send-keys -t requirement-analyst:0 "새 작업 할당됨: task-001.json"
         │    sleep 0.2
         │    tmux send-keys -t requirement-analyst:0 C-m
         │
         │ 5. 시그널 대기 (최대 6분)
         │    while [ ! -f signals/req-analysis-done ]; do
         │        sleep 2
         │        echo "대기 중... ($SECONDS초 경과)"
         │    done
         │
         │ 6. 시그널 파싱
         │    STATUS=$(grep "^status:" signals/req-analysis-done | cut -d: -f2)
         │    ARTIFACT=$(grep "^artifact:" signals/req-analysis-done | cut -d: -f2)
         │
         │ 7. 시그널 파일 삭제
         │    rm signals/req-analysis-done
         │
         └─> [다음 단계 진행]

┌────────────────────────┐
│  Requirement Analyst   │
└───────────┬────────────┘
            │
            │ 8. 작업 파일 감지 (tmux 메시지 수신)
            │    TASK_FILE=$(ls tasks/requirement-analyst/*.json | head -n 1)
            │
            │ 9. JSON 파싱
            │    INPUT=$(jq -r '.input' "$TASK_FILE")
            │    OUTPUT=$(jq -r '.output' "$TASK_FILE")
            │    CALLBACK=$(jq -r '.callback' "$TASK_FILE")
            │
            │ 10. 입력 파일 읽기
            │     USER_REQUEST=$(cat "$INPUT")
            │
            │ 11. 요구사항 분석 수행 (Gemini AI 사용)
            │
            │ 12. 결과 저장
            │     cat > "$OUTPUT" << 'RESULT'
            │     # 요구사항 분석 (초안)
            │     ...
            │     RESULT
            │
            │ 13. 시그널 전송
            │     cat > "$CALLBACK" << SIGNAL
            │     status:need_user_input
            │     artifact:$OUTPUT
            │     ...
            │     SIGNAL
            │
            │ 14. 정리
            │     rm "$TASK_FILE"
            │     echo "idle" > status/requirement-analyst.status
            │
            └─> [대기 상태로 복귀]
```

### 3.5 tmux send-keys 안정성 개선

**문제:** 긴 메시지와 Enter를 한 번에 전송하면 버퍼 문제로 Enter가 누락됨

**해결:** 메시지와 Enter(C-m)을 분리하여 전송

```bash
# Before (문제 있음)
tmux send-keys -t agent:0 "long message here" C-m

# After (안정적)
tmux send-keys -t agent:0 "long message here"
sleep 0.2
tmux send-keys -t agent:0 C-m
```

---

## 4. 전체 워크플로우

### 4.1 Phase 다이어그램

```
┌─────────────────────────────────────────────────────────────┐
│                     사용자 요청 입력                         │
└──────────────────────────┬──────────────────────────────────┘
                           │
                           ▼
┌───────────────────────────────────────────────────────────────┐
│  Phase 0: 요구사항 분석 (Requirement Analyst)                  │
│  - 사용자 요청 분석                                            │
│  - 불명확한 사항 질문 생성                                      │
│  → requirements-draft.md                                      │
│  → 시그널: status:need_user_input                             │
└──────────────────────────┬───────────────────────────────────┘
                           │
                     [사용자 답변 입력]
                           │
                           ▼
┌───────────────────────────────────────────────────────────────┐
│  Phase 1: 요구사항 확정 (Requirement Analyst)                  │
│  - 사용자 답변 통합                                            │
│  - 최종 요구사항 명세서 작성                                    │
│  → requirements.md                                            │
│  → 시그널: status:completed                                   │
└──────────────────────────┬───────────────────────────────────┘
                           │
                           ▼
┌───────────────────────────────────────────────────────────────┐
│  Phase 2: UX 설계 (UX Designer)                               │
│  - 입력: requirements.md                                      │
│  - 사용자 페르소나, 플로우, 와이어프레임 설계                    │
│  → ux-design.md                                               │
│  → 시그널: status:completed                                   │
└──────────────────────────┬───────────────────────────────────┘
                           │
                           ▼
┌───────────────────────────────────────────────────────────────┐
│  Phase 3: 기술 아키텍처 (Tech Architect)                       │
│  - 입력: requirements.md, ux-design.md                        │
│  - 기술 스택 선정, 아키텍처 설계, 폴더 구조                       │
│  → tech-spec.md                                               │
│  → 시그널: status:completed                                   │
└──────────────────────────┬───────────────────────────────────┘
                           │
                           ▼
┌───────────────────────────────────────────────────────────────┐
│  Phase 4: 구현 계획 (Planner)                                 │
│  - 입력: 모든 이전 문서                                        │
│  - Iteration으로 분할 (MVP → 기능 추가 → 폴리싱)                │
│  → implementation-plan.md                                     │
│  → 시그널: status:completed                                   │
└──────────────────────────┬───────────────────────────────────┘
                           │
                     [사용자 승인 대기]
                           │
                           ▼
┌───────────────────────────────────────────────────────────────┐
│  Phase 5: 테스트 설계 (Test Designer)                         │
│  - 입력: tech-spec.md, implementation-plan.md                 │
│  - TDD 방식으로 테스트 먼저 작성                                │
│  → test-plan.md, /workspace/tests/                           │
│  → 시그널: status:completed                                   │
└──────────────────────────┬───────────────────────────────────┘
                           │
                           ▼
┌───────────────────────────────────────────────────────────────┐
│  Phase 6: 구현 반복 (Developer + Reviewer)                    │
│                                                               │
│  ┌─────────────────────────────────────────┐                 │
│  │  Iteration 1                            │                 │
│  │  ├─ Developer: 테스트 확인              │                 │
│  │  ├─ Developer: 코드 작성                │                 │
│  │  │  → /workspace/project/[project]/    │                 │
│  │  ├─ Developer: 자체 테스트 실행         │                 │
│  │  └─ 시그널: dev-iter1-done              │                 │
│  │                                         │                 │
│  │  ├─ Reviewer: 코드 리뷰                 │                 │
│  │  ├─ Reviewer: 테스트 커버리지 확인      │                 │
│  │  └─ 시그널: review-iter1-done           │                 │
│  │     - status:approved → 다음 Iteration  │                 │
│  │     - status:rejected → Developer 수정  │                 │
│  └─────────────────────────────────────────┘                 │
│                                                               │
│  ┌─────────────────────────────────────────┐                 │
│  │  Iteration 2                            │                 │
│  │  ...                                    │                 │
│  └─────────────────────────────────────────┘                 │
│                                                               │
│  ┌─────────────────────────────────────────┐                 │
│  │  Iteration N (최종)                     │                 │
│  │  ...                                    │                 │
│  └─────────────────────────────────────────┘                 │
└──────────────────────────┬───────────────────────────────────┘
                           │
                           ▼
┌───────────────────────────────────────────────────────────────┐
│  Phase 7: 문서화 (Documenter)                                 │
│  - 입력: 완성된 코드                                           │
│  - README.md, ARCHITECTURE.md, API.md 생성                    │
│  → /workspace/project/[project]/                             │
│  → 시그널: status:completed                                   │
└──────────────────────────┬───────────────────────────────────┘
                           │
                           ▼
┌───────────────────────────────────────────────────────────────┐
│                     프로젝트 완료                              │
│  최종 결과물: /workspace/project/[타임스탬프]_[프로젝트명]/     │
└───────────────────────────────────────────────────────────────┘
```

### 4.2 Orchestrator의 단계별 작업

Orchestrator는 다음과 같이 **순차적으로** 각 Phase를 진행합니다:

```bash
# Phase 0: 요구사항 분석
1. 사용자 요청 저장
2. requirement-analyst에게 작업 지시
3. 시그널 대기 (status:need_user_input)
4. 사용자에게 질문 전달 및 답변 수집

# Phase 1: 요구사항 확정
5. requirement-analyst에게 finalize 작업 지시
6. 시그널 대기 (status:completed)

# Phase 2: UX 설계
7. ux-designer에게 작업 지시
8. 시그널 대기

# Phase 3: 기술 아키텍처
9. tech-architect에게 작업 지시
10. 시그널 대기

# Phase 4: 구현 계획
11. planner에게 작업 지시
12. 시그널 대기
13. 사용자 승인 요청

# Phase 5: 테스트 설계
14. test-designer에게 작업 지시
15. 시그널 대기

# Phase 6: 구현 반복
16. FOR EACH Iteration:
    a. developer에게 작업 지시
    b. 시그널 대기 (dev-iterN-done)
    c. reviewer에게 작업 지시
    d. 시그널 대기 (review-iterN-done)
    e. IF status:rejected THEN GOTO 16.a
    f. IF 모든 Iteration 완료 THEN GOTO 17

# Phase 7: 문서화
17. documenter에게 작업 지시
18. 시그널 대기
19. 완료 메시지 출력
```

---

## 5. 각 에이전트 상세

### 5.1 Orchestrator

**역할:** 중앙 제어, 전체 워크플로우 관리

**GEMINI.md 핵심 지시문:**

```
당신은 중앙 제어 오케스트레이터입니다.

핵심 역할:
1. 워크플로우 관리: 전체 개발 프로세스를 단계별로 진행
2. 에이전트 조율: 각 에이전트에게 작업 지시 및 결과 수신
3. 상태 추적: 프로젝트 진행 상태 모니터링
4. 사용자 인터랙션: 필요 시 사용자 승인 요청

중요 규칙:
1. 순차 실행: 반드시 이전 단계 완료 후 다음 진행
2. 상태 확인: 작업 지시 전 에이전트가 idle 상태인지 확인
3. 로그 기록: 모든 작업을 /workspace/logs/orchestrator.log에 기록
4. 사용자 우선: 사용자 승인이 필요한 시점에는 반드시 대기
```

**프로젝트 초기화 코드:**

```bash
# 1. 프로젝트 ID 생성
PROJECT_ID=$(date +%Y%m%d_%H%M%S)_$(echo "$USER_REQUEST" | md5sum | cut -c1-8)
echo "$PROJECT_ID" > /workspace/status/current_project.id
echo "$USER_REQUEST" > /workspace/input/user_request.txt
```

**에이전트 작업 지시 패턴:**

```bash
# 1. 상태 확인
while [ "$(cat /workspace/status/requirement-analyst.status)" != "idle" ]; do
    sleep 1
done

# 2. 작업 파일 생성
cat > /workspace/tasks/requirement-analyst/task-001.json << TASK
{
  "task_id": "req-analysis-001",
  "command": "analyze_requirements",
  "input": "/workspace/input/user_request.txt",
  "output": "/workspace/artifacts/requirements-draft.md",
  "callback": "/workspace/signals/req-analysis-done"
}
TASK

# 3. 상태 업데이트
echo "working" > /workspace/status/requirement-analyst.status

# 4. tmux 알림 전송 (메시지와 Enter 분리)
tmux send-keys -t requirement-analyst:0 "새로운 작업이 할당되었습니다. /workspace/tasks/requirement-analyst/task-001.json 파일을 확인하세요."
sleep 0.2
tmux send-keys -t requirement-analyst:0 C-m

# 5. 시그널 대기 (Bash tool 내에서 실행, 최대 6분 타임아웃)
while [ ! -f /workspace/signals/req-analysis-done ]; do
    sleep 2
    SECONDS=$((SECONDS + 2))
    echo "대기 중... (${SECONDS}초 경과)"
done

# 6. 시그널 파싱
STATUS=$(grep "^status:" /workspace/signals/req-analysis-done | cut -d: -f2)
ARTIFACT=$(grep "^artifact:" /workspace/signals/req-analysis-done | cut -d: -f2)

# 7. 시그널 파일 삭제
rm /workspace/signals/req-analysis-done

# 8. 다음 단계 진행
if [ "$STATUS" == "completed" ]; then
    # 다음 에이전트로
elif [ "$STATUS" == "need_user_input" ]; then
    # 사용자에게 질문
fi
```

---

### 5.2 Requirement Analyst

**역할:** 요구사항 분석 및 명확화

**GEMINI.md 핵심 지시문:**

```
당신은 요구사항 분석 전문가입니다.
사용자의 모호한 요청을 명확한 요구사항으로 정리합니다.

역할: 초기 사용자 요청을 분석하고 불명확한 부분을 질문으로 정리합니다.
```

**작업 프로세스:**

```bash
# 1. 작업 파일 읽기
TASK_FILE=$(ls /workspace/tasks/requirement-analyst/*.json | head -n 1)

if [ -n "$TASK_FILE" ]; then
    echo "📥 새 작업 수신: $TASK_FILE"

    # JSON 파싱
    INPUT=$(jq -r '.input' "$TASK_FILE")
    OUTPUT=$(jq -r '.output' "$TASK_FILE")
    CALLBACK=$(jq -r '.callback' "$TASK_FILE")
fi

# 2. 사용자 요청 분석
USER_REQUEST=$(cat "$INPUT")
echo "분석 중: $USER_REQUEST"

# 3. 요구사항 초안 작성 (Gemini AI 사용)
# ...

# 4. 결과 저장
cat > "$OUTPUT" << 'RESULT'
# 요구사항 분석 (초안)

## 사용자 요청
[원본 요청]

## 파악된 요구사항
- 기능 1: ...
- 기능 2: ...

## 불명확한 사항 - 사용자 확인 필요 ❓

### 1. [질문 카테고리]
**질문**: [구체적인 질문]
**이유**: [왜 필요한지]
**옵션**:
- A) [선택지 1]
- B) [선택지 2]

## 제안 사항
- [전문가 추천]
RESULT

# 5. 시그널 전송
cat > "$CALLBACK" << SIGNAL
status:need_user_input
artifact:$OUTPUT
question_count:7
timestamp:$(date -Iseconds)
SIGNAL

# 6. 정리
rm "$TASK_FILE"
echo "idle" > /workspace/status/requirement-analyst.status
```

**요구사항 확정 (finalize) 작업:**

사용자 답변을 받은 후, Orchestrator가 다시 작업 지시:

```bash
# Orchestrator가 전달하는 작업 파일
{
  "task_id": "req-finalize-001",
  "command": "finalize_requirements",
  "input": "/workspace/input/user_answers.txt",
  "draft": "/workspace/artifacts/requirements-draft.md",
  "output": "/workspace/artifacts/requirements.md",
  "callback": "/workspace/signals/req-finalize-done"
}
```

최종 산출물 형식:

```markdown
# 최종 요구사항 명세서

## 프로젝트 개요
[1-2문장 요약]

## 기능 요구사항
### FR-1: [기능명]
- 설명: [상세 설명]
- 우선순위: High/Medium/Low
- 사용자 스토리: As a [user], I want [feature] so that [benefit]

## 비기능 요구사항
- 성능: [예: 로딩 시간 < 2초]
- 접근성: [예: WCAG 2.1 AA]
- 브라우저: [지원 범위]

## 제약사항
- [기술적/비즈니스적 제약]

## 성공 기준
- [ ] [측정 가능한 목표 1]
- [ ] [측정 가능한 목표 2]
```

---

### 5.3 UX Designer

**역할:** 사용자 경험 및 인터페이스 설계

**GEMINI.md 핵심 지시문:**

```
당신은 UX/UI 설계 전문가입니다.
확정된 요구사항을 바탕으로 사용자 플로우와 인터페이스를 설계합니다.
```

**산출물 형식:**

```markdown
# UX 설계 문서

## 1. 사용자 페르소나
### 주요 사용자
- 이름: [페르소나명]
- 특성: [설명]
- 목표: [사용 목적]
- Pain Points: [해결해야 할 문제]

## 2. 사용자 플로우
```
[진입] → [액션1] → [액션2] → [목표 달성]
       ↓ (오류 시)
       [오류 처리] → [복구]
```

## 3. 화면 구성 (와이어프레임)

### 메인 화면
```
+----------------------------------+
|  [Header/Title]                  |
+----------------------------------+
|                                  |
|  [Main Content Area - 70%]       |
|                                  |
+----------------------------------+
|  [Control Panel - 30%]           |
|  [Button] [Button]               |
+----------------------------------+
```

## 4. 인터랙션 정의
- 액션: 클릭/터치/드래그
- 피드백: 시각적/청각적 피드백
- 트랜지션: 애니메이션 명세

## 5. 접근성 고려사항
- 키보드 네비게이션
- 스크린 리더 지원
- 색각 이상 대응

## 6. 반응형 설계
- Desktop: [사양]
- Tablet: [조정사항]
- Mobile: [조정사항]
```

**완료 시그널:**

```bash
cat > /workspace/signals/ux-design-done << 'SIGNAL'
status:completed
artifact:/workspace/artifacts/ux-design.md
confidence:high
timestamp:$(date -Iseconds)
SIGNAL
```

---

### 5.4 Tech Architect

**역할:** 기술 스택 선정 및 아키텍처 설계

**GEMINI.md 핵심 지시문:**

```
당신은 기술 아키텍처 설계자입니다.
요구사항과 UX 설계를 바탕으로 기술 스택과 아키텍처를 설계합니다.
```

**산출물 형식:**

```markdown
# 기술 명세서

## 1. 기술 스택
### Frontend
- 프레임워크: [React/Vue/Svelte]
- 주요 라이브러리:
  - [라이브러리명] (버전) - [용도]
  - [번들 크기]

### 의존성 분석
- 총 번들 크기: [예상 크기]
- 초기 로딩 시간: [예상]
- 대안 검토:
  - Option A: [장단점]
  - Option B: [장단점]
  - ✅ 선택: [이유]

## 2. 아키텍처
### 폴더 구조
```
src/
  components/
    [컴포넌트명]/
      index.jsx
      styles.css
  hooks/
  utils/
```

### 데이터 플로우
```
User Action → Event Handler → State Update → Re-render
```

## 3. 성능 고려사항
- [최적화 전략]
- [병목 지점 분석]

## 4. 리스크 분석
⚠️ Risk 1: [설명]
   - 영향도: High/Medium/Low
   - 완화 방안: [대응책]

## 5. 브라우저 지원
- Chrome: [버전]
- Firefox: [버전]
- Safari: [버전]
- Edge: [버전]
```

---

### 5.5 Planner

**역할:** 구현을 단계별 Iteration으로 분할

**GEMINI.md 핵심 지시문:**

```
당신은 구현 계획 수립자입니다.
전체 구현을 단계별 Iteration으로 나눕니다.
```

**산출물 형식:**

```markdown
# 구현 계획서

## Iteration 1: MVP (예상: 1-2시간)
### 목표
최소 기능 프로토타입 완성

### 작업 목록
- [ ] Task 1.1: [작업명]
  - 설명: [상세]
  - 예상 시간: 30분
  - 의존성: 없음

- [ ] Task 1.2: ...

### 검증 기준
- [ ] 기준 1: [측정 가능한 기준]
- [ ] 기준 2: ...

### 완료 조건
사용자가 [핵심 기능]을 사용할 수 있다.

---

## Iteration 2: 핵심 기능 (예상: 1-2시간)
...

---

## Iteration 3: 폴리싱 (예상: 1시간)
### 목표
사용자 경험 개선

### 작업 목록
- [ ] 애니메이션 추가
- [ ] 에러 처리
- [ ] 접근성 개선

---

## 전체 타임라인
```
Week 1: [Iteration 1-2]
Week 2: [Iteration 3 + 테스트]
```

## 리스크 관리
- Risk: [설명]
- 완화: [대응책]
```

---

### 5.6 Test Designer

**역할:** TDD 방식으로 테스트 먼저 작성

**GEMINI.md 핵심 지시문:**

```
당신은 테스트 설계 전문가입니다.
각 Iteration 전에 테스트를 작성합니다 (TDD 방식).
```

**산출물 예시:**

```javascript
// tests/DiceScene.test.jsx
import { render, screen } from '@testing-library/react';
import DiceScene from '../components/DiceScene';

describe('DiceScene', () => {
  test('주사위가 렌더링됨', () => {
    render(<DiceScene />);
    const canvas = screen.getByTestId('dice-canvas');
    expect(canvas).toBeInTheDocument();
  });

  test('Roll 버튼 클릭 시 애니메이션 시작', () => {
    // ...
  });
});
```

**완료 시그널:**

```bash
cat > /workspace/signals/tests-iter1-done << 'SIGNAL'
status:completed
artifacts:/workspace/tests/DiceScene.test.jsx
test_count:5
timestamp:$(date -Iseconds)
SIGNAL
```

---

### 5.7 Developer

**역할:** 실제 코드 구현

**GEMINI.md 핵심 지시문:**

```
당신은 소프트웨어 개발자입니다.
계획에 따라 실제 코드를 작성합니다.

작업 방식:
1. 테스트 확인: 먼저 작성된 테스트 읽기
2. 단계별 구현: 한 번에 하나씩
3. 체크포인트: 15분마다 진행 상황 로그
4. 자체 검증: 각 함수 완성 후 테스트 실행
```

**구현 로그 형식:**

```markdown
# Implementation Log - Iteration 1

## [10:35] 시작
- 목표: MVP 완성
- 예상 소요: 1시간

## [10:45] DiceScene.jsx 기본 구조
- Three.js 씬 초기화 완료
- ✅ 테스트 1/5 통과

## [10:55] 주사위 렌더링
- BoxGeometry 추가
- ✅ 테스트 3/5 통과

## [11:05] Roll 버튼
- 이벤트 핸들러 구현
- ✅ 테스트 5/5 통과

## [11:05] 완료
- 모든 테스트 통과
- 다음: reviewer에게 전달
```

**완료 시그널:**

```bash
cat > /workspace/signals/dev-iter1-done << 'SIGNAL'
status:iteration_complete
iteration:1
tests_passed:5/5
artifacts:/workspace/src/
timestamp:$(date -Iseconds)
SIGNAL
```

---

### 5.8 Reviewer

**역할:** 코드 리뷰 및 품질 검증

**GEMINI.md 핵심 지시문:**

```
당신은 코드 리뷰어입니다.
구현된 코드를 검토하고 품질을 보증합니다.
```

**리뷰 체크리스트:**

```
### 설계 준수
- [ ] tech-spec의 아키텍처를 따르는가?
- [ ] 폴더 구조가 일치하는가?
- [ ] 의존성이 올바른가?

### 코드 품질
- [ ] 린트 통과 (ESLint)
- [ ] 명명 규칙 준수
- [ ] 주석이 적절한가?
- [ ] 컴포넌트 크기 (<200 lines)

### 기능 검증
- [ ] 모든 테스트 통과
- [ ] 요구사항 충족
- [ ] 엣지 케이스 처리

### 성능
- [ ] 불필요한 리렌더링 없음
- [ ] 메모리 누수 없음
```

**리뷰 결과 형식:**

```markdown
# Code Review - Iteration 1

## ✅ 통과 항목
- 모든 테스트 통과 (5/5)
- 설계 준수
- 코드 품질 양호

## ⚠️ 개선 제안 (블로킹 아님)
1. DiceScene.jsx:45 - Consider extracting roll logic to custom hook
   - 이유: 재사용성 향상
   - 우선순위: Low

## ❌ 블로킹 이슈
없음

## 결론
✅ Iteration 1 승인 - 다음 단계 진행 가능
```

**시그널:**

```bash
# 승인 시
cat > /workspace/signals/review-iter1-done << 'SIGNAL'
status:approved
blocking_issues:0
warnings:1
SIGNAL

# 거부 시
cat > /workspace/signals/review-iter1-done << 'SIGNAL'
status:rejected
blocking_issues:2
required_changes:/workspace/reviews/changes-required.md
SIGNAL
```

---

### 5.9 Documenter

**역할:** 프로젝트 문서화

**GEMINI.md 핵심 지시문:**

```
당신은 기술 문서 작성자입니다.
프로젝트 완료 후 종합 문서를 작성합니다.
```

**생성 문서:**

1. **README.md**

```markdown
# [프로젝트명]

## 소개
[1-2문장 설명]

## 기능
- 기능 1
- 기능 2

## 기술 스택
- React 18
- Three.js
- Cannon.js

## 시작하기
```bash
npm install
npm run dev
```

## 프로젝트 구조
...

## 라이선스
MIT
```

2. **ARCHITECTURE.md** - 시스템 아키텍처 상세 설명
3. **API.md** - 컴포넌트/함수 API 레퍼런스
4. **CHANGELOG.md** - 개발 히스토리

**완료 시그널:**

```bash
cat > /workspace/signals/docs-done << 'SIGNAL'
status:completed
artifacts:/workspace/docs/README.md,/workspace/docs/ARCHITECTURE.md
timestamp:$(date -Iseconds)
SIGNAL
```

---

## 6. 파일 시스템 구조

### 6.1 전체 디렉토리 구조

```
MAS/
├── run.sh                          # 메인 실행 스크립트 (메뉴 기반)
├── setup.sh                        # 의존성 확인 및 초기 설정
├── config.sh                       # 시스템 설정 (모델, 포트 등)
│
├── scripts/
│   ├── setup-workspace.sh          # 워크스페이스 초기화
│   ├── setup-agents.sh             # 에이전트 GEMINI.md 생성
│   ├── cleanup-sessions.sh         # tmux 세션 정리
│   ├── start-sessions.sh           # 에이전트 세션 시작
│   ├── start-sessions-auto.sh      # 자동화 모드 세션 시작
│   ├── clean-workspace.sh          # 작업 결과물 초기화
│   ├── cleanup-phase.sh            # 단계별 에이전트 종료
│   ├── view-all-agents.sh          # 9개 에이전트 동시 모니터링
│   └── stop-all.sh                 # 전체 종료
│
├── packages/
│   └── dashboard/                  # 웹 대시보드
│       ├── server.js               # 프록시 서버
│       ├── index.html              # 대시보드 UI
│       ├── js/                     # 프론트엔드 로직
│       ├── css/                    # 스타일
│       ├── assets/                 # 정적 리소스
│       ├── start-dashboard.sh      # 대시보드 시작
│       ├── start-dashboard-auto.sh # 대시보드 시작 (자동)
│       └── stop-dashboard.sh       # 대시보드 종료
│
└── workspace/
    ├── agents/                     # 각 에이전트 작업 디렉토리
    │   ├── orchestrator/
    │   │   └── GEMINI.md           # 시스템 프롬프트
    │   ├── requirement-analyst/
    │   ├── ux-designer/
    │   ├── tech-architect/
    │   ├── planner/
    │   ├── test-designer/
    │   ├── developer/
    │   ├── reviewer/
    │   └── documenter/
    │
    ├── artifacts/                  # 중간 산출물
    │
    ├── callbacks/                  # 콜백 파일
    │
    ├── input/                      # 사용자 입력
    │
    ├── logs/                       # 실행 로그
    │
    ├── project/                    # 프로젝트 작업 공간 (최종 결과물)
    │
    ├── reviews/                    # 코드 리뷰 결과
    │
    ├── signals/                    # 통신용 시그널
    │
    ├── state/                      # 상태 저장
    │
    ├── status/                     # 에이전트 상태
    │
    ├── tasks/                      # 에이전트별 작업 큐
    │
    ├── tests/                      # 임시 테스트 파일
    ├── docs/                       # 임시 문서
    └── src/                        # 임시 소스 코드
```

### 6.2 실행 모드 비교

run.sh 메뉴에서 선택할 수 있는 모드:

| 메뉴 | 에이전트 수 | 권한 모드 | 용도 |
|------|-------------|-----------|------|
| 1) 터미널 실행 | 9개 전체 | 수동 승인 | 새 프로젝트 (단계별 확인) |
| 2) 터미널 실행 (Auto) | 9개 전체 | `--dangerously-skip-permissions` | 새 프로젝트 (완전 자동) |
| 3) 웹 대시보드 | 9개 전체 | 수동 승인 | 웹 기반 모니터링 |
| 4) 웹 대시보드 (Auto) | 9개 전체 | `--dangerously-skip-permissions` | 웹 + 완전 자동 |

### 6.3 workspace 초기화 정책

**매 실행 시 초기화되는 디렉토리:**
- `artifacts/` - 중간 산출물
- `input/` - 사용자 입력
- `signals/` - 시그널 파일
- `logs/` - 로그
- `src/`, `tests/`, `docs/` - 임시 파일
- `state/`, `callbacks/` - 상태 및 콜백 파일
- `tasks/` - 작업 큐 (JSON 파일만 삭제, 폴더는 유지)
- `status/` - 모두 "idle"로 리셋, 프로젝트 관련 파일 삭제

**영구 보관되는 디렉토리:**
- `agents/` - 에이전트 GEMINI.md 파일
- `project/` - 완성된 프로젝트 결과물

---

## 7. 토큰 소모 및 성능

### 7.1 토큰 소모 패턴

**토큰이 소모되는 시점:**
1. 사용자가 메시지를 보낼 때
2. Gemini가 응답을 생성할 때
3. Tool을 호출하고 결과를 받을 때

**토큰이 소모되지 않는 시점:**
- Gemini 세션이 단순히 살아있을 때
- Bash tool 내부에서 반복문이 실행될 때 (while loop)

### 7.2 Orchestrator의 시그널 대기

**Bash tool 실행:**

```bash
# 이 전체 블록이 Bash tool 1회 호출
while [ ! -f /workspace/signals/req-analysis-done ]; do
    sleep 2
    SECONDS=$((SECONDS + 2))
    echo "대기 중... (${SECONDS}초 경과)"
done
```

**토큰 소모:**
- Bash tool 호출 시: **1회 소모**
- 반복문이 몇 번 돌든: **추가 소모 없음**
- Bash tool 완료 후 결과 반환: **1회 소모**

**타임아웃:**
- Gemini가 자동으로 설정: **6분 (360초)**
- 6분 안에 시그널이 오지 않으면: Bash tool 실패, 에러 반환
- 타임아웃 발생 시: Gemini가 에러 응답 생성 (토큰 소모), 이후 세션 멈춤

### 7.3 전체 프로세스 토큰 소모 예상

**단계별 에이전트 호출 횟수:**

| Phase | 에이전트 | Bash tool 호출 | Gemini 응답 | 예상 토큰 |
|-------|----------|----------------|-------------|----------|
| 0 | Requirement Analyst | 5회 | 2회 | ~3K |
| 1 | Requirement Analyst (finalize) | 5회 | 1회 | ~2K |
| 2 | UX Designer | 5회 | 1회 | ~2K |
| 3 | Tech Architect | 5회 | 1회 | ~2K |
| 4 | Planner | 5회 | 1회 | ~2K |
| 5 | Test Designer | 5회 | 1회 | ~2K |
| 6 | Developer (Iteration 1) | 10회 | 3회 | ~5K |
| 6 | Reviewer (Iteration 1) | 8회 | 2회 | ~3K |
| 6 | Developer (Iteration 2) | 10회 | 3회 | ~5K |
| 6 | Reviewer (Iteration 2) | 8회 | 2회 | ~3K |
| 7 | Documenter | 5회 | 1회 | ~2K |
| - | Orchestrator (전체 조율) | 30회 | 15회 | ~10K |

**전체 예상:** ~41K 토큰 (3 Iteration 기준)

---

## 부록

### A. 트러블슈팅

**문제: tmux 메시지가 입력되지 않음**

```bash
# 원인: Enter 키가 전달되지 않음
# 해결: 메시지와 Enter를 분리
tmux send-keys -t agent:0 "message"
sleep 0.2
tmux send-keys -t agent:0 C-m
```

**문제: 에이전트가 작업을 시작하지 않음**

```bash
# 1. tmux 세션 확인
tmux list-sessions

# 2. 에이전트 상태 확인
cat workspace/status/requirement-analyst.status

# 3. 작업 파일 확인
ls workspace/tasks/requirement-analyst/

# 4. 에이전트 세션에 직접 접속하여 확인
tmux attach-session -t requirement-analyst
```

**문제: 타임아웃 발생**

```bash
# 원인: 6분 안에 작업이 완료되지 않음
# 해결: 작업을 더 작은 단위로 분할하거나,
#       복잡한 작업의 경우 Developer를 여러 Iteration으로 나눔
```

### B. 시스템 확장 가이드

**새 에이전트 추가:**

1. `workspace/agents/new-agent/GEMINI.md` 생성
2. `scripts/setup-agents.sh`에 에이전트 추가
3. `scripts/start-sessions.sh`에 세션 시작 로직 추가
4. Orchestrator의 워크플로우에 Phase 추가

**새 통신 패턴 추가:**

1. 기존: 1:1 통신 (Orchestrator ↔ Agent)
2. 확장: 에이전트 간 직접 통신 (예: Developer → Reviewer 직접 호출)

```bash
# Developer가 직접 Reviewer 호출
cat > /workspace/tasks/reviewer/task-001.json << TASK
{
  "task_id": "review-iter1",
  "command": "review_code",
  "input": "/workspace/project/[project]/",
  "callback": "/workspace/signals/review-iter1-done"
}
TASK

tmux send-keys -t reviewer:0 "새 작업: task-001.json"
sleep 0.2
tmux send-keys -t reviewer:0 C-m
```

---

**문서 끝**
